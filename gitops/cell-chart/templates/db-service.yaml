apiVersion: v1
kind: Service
metadata:
  name: cell-db
  namespace: cell-{{ .Values.cellId }}
spec:
  clusterIP: None
  selector:
    app: cell-db
    cell-id: "{{ .Values.cellId }}"
  ports:
    - port: 5432
      targetPort: 5432
