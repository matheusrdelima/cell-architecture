# Valores padrão de uma célula. Cada célula sobrescreve o que precisa
# no arquivo gitops/cells/<nome-da-celula>/values.yaml

cellId: "default"

# Usado apenas pelo ApplicationSet para decidir o cluster de destino
# (não é consumido por nenhum template do chart).
targetShard: "shard-1"

appImage: "python:3.12-slim"
appReplicas: 1

service:
  port: 8080

db:
  image: "postgres:16-alpine"
  name: "cellsdb"
  user: "celluser"
  password: "cellpass"
  storage: "1Gi"

gateway:
  pathPrefix: "/cell-default"
